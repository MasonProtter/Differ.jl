var documenterSearchIndex = {"docs":
[{"category":"section","location":"reference/#API-Reference","page":"API Reference","text":"This page documents Differ's public API, grouped by topic rather than dumped alphabetically. using Differ re-exports everything below; the individual sub-packages (Contextual/DifferCore/DifferForwards/DifferReverse) also export it directly if you only depend on one of them.\n\nEach package has plenty of internal, non-exported functions with their own docstrings (accessible via ?somename in the REPL) that aren't listed here — they're implementation detail, not part of the API surface. See Differ/.claude/skills/ (differ-architecture and friends) for a guided tour of those internals.","title":"API Reference"},{"category":"section","location":"reference/#Tangents,-fdata,-and-rdata-(DifferCore)","page":"API Reference","text":"The primal-type-keyed tangent system shared by both AD modes. Every primal type P has exactly one tangent type tangent_type(P), which splits further into an fdata half (threaded forward and incremented in place) and an rdata half (only accumulable during a reverse pass) — DifferReverse's CoDual carries the fdata half; DifferForwards's Dual carries the whole tangent, since forward mode has no need for the split.","title":"Tangents, fdata, and rdata (DifferCore)"},{"category":"section","location":"reference/#Activity-marking-(DifferCore)","page":"API Reference","text":"Inactive marks a value the caller holds constant: no derivative is propagated to or from it. It is a legal inhabitant of a shadow slot alongside the primal-derived one — fdata_shadow_type(P) gives that legal set for reverse mode's CoDual, tangent_shadow_type(P) for forward mode's Dual (which carries the whole tangent rather than the fdata half). shadow_type is a deprecated alias for the former. Inactive is distinct from NoTangent, which says a type has no tangent space rather than that a particular value was declared constant; see the Inactive docstring for why the two can't be merged.\n\nBoth modes carry it. In reverse mode a CoDual's shadow can be Inactive, and build_ctx accepts it both via its inactive= positional-index form and by reading it straight off CoDual carriers. In forward mode a Dual's tangent slot can be Inactive — frule!!(…, Dual(w, Inactive())) — and code_dual_ircode takes the same inactive= position list. Everything reachable only through constants is replayed primally with no tangent computed at all. A constant read by an active computation is handed to the rule as Inactive() where the rule can take one, so the term is skipped rather than multiplied by a zero; elsewhere a zero is materialised at the constant's definition. A statically-known code constant — a literal, a QuoteNode, a const GlobalRef — is minted Inactive too when its tangent carries no fdata (inactive_constant_type), since nothing can write to it and its shadow could only ever stay zero. Mutable state behind a constant keeps a real zero shadow, because a write inside the differentiated function updates it. Hand-written rules in both modes accept Inactive in any differentiable slot. A mutating rule (mul!, map!, setindex!) refuses an inactive destination rather than silently dropping the derivative flowing from its sources.","title":"Activity marking (DifferCore)"},{"category":"section","location":"reference/#Forward-mode-(DifferForwards)","page":"API Reference","text":"Dual pairs a primal value with its full tangent — or with Inactive for an argument the caller holds constant; see Activity marking. frule!! is the forward-mode rule, dispatched on Dual arguments, with an @generated fallback that derives a rule for any composite function from its IR.","title":"Forward mode (DifferForwards)"},{"category":"section","location":"reference/#Reverse-mode-(DifferReverse)","page":"API Reference","text":"CoDual pairs a primal value with its fdata — or with Inactive for an argument the caller holds constant; see Activity marking. rrule!! is the reverse-mode rule — hand-written primitives and an @generated derived fallback are both methods of the same generic function. Ctx (built via build_ctx) carries the pullback Tape between calls so it can be reused instead of reallocated. rev_gradient/rev_gradient!/fcodual_type/ codual_type are public rather than exported — reach them as DifferReverse.rev_gradient(...), etc. — since DifferentiationInterface.jl is the primary user-facing entry point now.","title":"Reverse mode (DifferReverse)"},{"category":"section","location":"reference/#DifferentiationInterface.jl-integration","page":"API Reference","text":"Dispatch structs selecting Differ's forward/reverse mode through DifferentiationInterface.jl — the recommended way to call into Differ; see the Home page for usage examples.","title":"DifferentiationInterface.jl integration"},{"category":"section","location":"reference/#Compiler-plugin-machinery-(Contextual)","page":"API Reference","text":"Mode-agnostic machinery both AD engines are built on top of: a generic AbstractInterpreter that lets a plugin swap in a transformed IRCode for a method during compilation, with no AD logic of its own. Relevant if you're extending Differ's engines or building a similar IR-transforming compiler plugin; not needed to just use forward/reverse mode.","title":"Compiler-plugin machinery (Contextual)"},{"category":"module","location":"reference/#Differ","page":"API Reference","text":"Differ\n\nThin re-export meta-package: using Differ pulls in Contextual, DifferCore, DifferForwards, and DifferReverse together, reproducing the pre-split monolith's combined namespace for existing using Differ consumers. No logic of its own — every real implementation lives in one of the four sub-packages, each independently installable and usable on its own. primal/tangent are single generic functions owned by DifferCore, with DifferForwards adding the Dual method and DifferReverse adding the CoDual one, so they're already unified without Differ needing to do anything.\n\nForward-over-reverse (D(x -> rev_gradient(f, x), v) and friends) works: load DifferForwards and DifferReverse in either order, and DifferForwardsOverReverseExt wires the two engines together.\n\n\n\n\n\n","title":"Differ"},{"category":"type","location":"reference/#DifferCore.NoTangent","page":"API Reference","text":"NoTangent\n\nThe type in question has no meaningful notion of a tangent space. Generally, you shouldn't use this – just let the default recursive tangent construction work. You might need to use this for primitive types though.\n\n\n\n\n\n","title":"DifferCore.NoTangent"},{"category":"type","location":"reference/#DifferCore.Tangent","page":"API Reference","text":"Tangent{Tfields<:NamedTuple}\n\nDefault type used to represent the tangent of a struct. See tangent_type for more info.\n\n\n\n\n\n","title":"DifferCore.Tangent"},{"category":"type","location":"reference/#DifferCore.MutableTangent","page":"API Reference","text":"MutableTangent{Tfields<:NamedTuple}\n\nDefault type used to represent the tangent of a mutable struct. See tangent_type for more info.\n\n\n\n\n\n","title":"DifferCore.MutableTangent"},{"category":"type","location":"reference/#DifferCore.PossiblyUninitTangent","page":"API Reference","text":"PossiblyUninitTangent{T}\n\nRepresents a T that may or may not be present. Does not distinguish between zero and not being present.\n\n\n\n\n\n","title":"DifferCore.PossiblyUninitTangent"},{"category":"type","location":"reference/#DifferCore.NoFData","page":"API Reference","text":"NoFData\n\nSingleton indicating there is nothing to propagate on the forwards-pass beyond the primal data.\n\n\n\n\n\n","title":"DifferCore.NoFData"},{"category":"type","location":"reference/#DifferCore.FData","page":"API Reference","text":"FData(data::NamedTuple)\n\nThe component of a struct which is propagated alongside the primal on the forwards-pass of AD. For example, the tangents for Float64s do not need to be propagated on the forwards- pass of reverse-mode AD, so any Float64 fields of Tangent do not need to appear in the associated FData.\n\n\n\n\n\n","title":"DifferCore.FData"},{"category":"type","location":"reference/#DifferCore.NoRData","page":"API Reference","text":"NoRData()\n\nNothing to propagate backwards on the reverse-pass.\n\n\n\n\n\n","title":"DifferCore.NoRData"},{"category":"type","location":"reference/#DifferCore.RData","page":"API Reference","text":"RData(data::NamedTuple)\n\nThe component of a struct's tangent which is not propagated on the forwards-pass of reverse- mode AD, produced only going backwards by a pullback — the complement of FData. For example, a Float64 field's tangent has no fdata, so it is entirely rdata.\n\n\n\n\n\n","title":"DifferCore.RData"},{"category":"type","location":"reference/#DifferCore.LazyZeroRData","page":"API Reference","text":"LazyZeroRData{P, Tdata}()\n\nLazy placeholder for zero_like_rdata_from_type, deferring construction of the zero data to the reverse pass. Calling instantiate on an instance produces the zero data.\n\nConstruct via LazyZeroRData(p) for p::P. Both the constructor and instantiate are specialised to minimise stored data — e.g. Float64s need none, so LazyZeroRData(0.0) is a singleton instance, enabling further optimisations in AD.\n\n\n\n\n\n","title":"DifferCore.LazyZeroRData"},{"category":"function","location":"reference/#DifferCore.tangent_type","page":"API Reference","text":"tangent_type(P)\n\nEach primal type P has a single tangent type, given by tangent_type(P).\n\nWarning: this function assumes the effects :removable and :consistent, which is necessary for performance but imposes precise constraints on your implementation. If adding new methods to tangent_type, consult the extended help of Base.@assume_effects to see what this imposes.\n\nExtended help\n\nMooncake.jl's tangent types are similar in spirit to ChainRules.jl. For example, tangent \"vectors\" for\n\nFloat64s are Float64s,\nVector{Float64}s are Vector{Float64}s, and\nstructs are another (special) struct with field types specified recursively.\n\nThere are major differences, though. Those tangent types are permissible in ChainRules.jl, but not uniquely so: ZeroTangent is also a valid tangent for any of them, and Float32 is valid for Float64. ChainRules.jl intentionally declines to restrict what type can represent the tangent of a given type.\n\nMooncake.jl differs from this. It insists that each primal type is associated to a single tangent type. Furthermore, this type is always given by the function DifferCore.tangent_type(primal_type).\n\nSome worked examples.\n\nInt\n\nInt is not a differentiable type, so its tangent type is NoTangent:\n\njulia> tangent_type(Int)\nNoTangent\n\nTuples\n\nThe tangent type of a Tuple is defined recursively based on its field types. For example\n\njulia> tangent_type(Tuple{Float64, Vector{Float64}, Int})\nTuple{Float64, Vector{Float64}, NoTangent}\n\nEdge case: if all fields of a Tuple are non-differentiable, the tangent type is NoTangent. For example,\n\njulia> tangent_type(Tuple{Int, Int})\nNoTangent\n\nStructs\n\nAs with Tuples, the tangent type of a struct is given recursively by default: it's Tangent, which wraps a NamedTuple containing the tangent for each field.\n\nAs with Tuples, if all field types are non-differentiable, the tangent type of the entire struct is NoTangent.\n\nTwo subtleties beyond Tuples. First, not all fields of a struct have to be defined; Julia tracks how many fields are always defined, and the tangent for any field that might not be is wrapped in a PossiblyUninitTangent.\n\nSecond, structs can have fields with abstract static types. For example\n\njulia> struct Foo\n           x\n       end\n\nIf you ask for the tangent type of Foo, you will see that it is\n\njulia> tangent_type(Foo)\nTangent{@NamedTuple{x}}\n\nThe field type for x is Any, because: x could have any type at runtime, so its tangent type isn't known until runtime; and the tangent type of Foo must be unique. Together these mean Foo's tangent must be able to hold any tangent type in its x field, so that field's type has to be Any.\n\nMutable Structs\n\nThe tangent type for mutable structs follows the same considerations as structs, except it must itself be mutable. We use MutableTangent, a mutable struct with the same shape as Tangent.\n\nFor example, if you ask for the tangent_type of\n\njulia> mutable struct Bar\n           x::Float64\n       end\n\nyou will find that it is\n\njulia> tangent_type(Bar)\nMutableTangent{@NamedTuple{x::Float64}}\n\nPrimitive Types\n\nWe've already seen two primitive types (Float64 and Int). Every primitive type requires an explicit tangent_type method.\n\nThe tangent type of a Ptr{P} is Ptr{T}, where T = tangent_type(P). For example\n\njulia> tangent_type(Ptr{Float64})\nPtr{Float64}\n\n\n\n\n\n","title":"DifferCore.tangent_type"},{"category":"function","location":"reference/#DifferCore.fdata_type","page":"API Reference","text":"fdata_type(T)\n\nReturns the type of the forwards data associated to a tangent of type T.\n\nExtended help\n\nRules in Mooncake.jl do not operate on tangents directly. Rather, functionality is defined to split each tangent into two components, that we call fdata (forwards-pass data) and rdata (reverse-pass data). In short, any component of a tangent which is identified by its address (e.g. a mutable structs or an Array) gets passed around on the forwards-pass of AD and is incremented in-place on the reverse-pass, while components of tangents identified by their value get propagated and accumulated only on the reverse-pass.\n\nGiven a tangent type T, you can find out what type its fdata and rdata must be with fdata_type(T) and rdata_type(T) respectively. A consequence of this is that there is exactly one valid fdata type and rdata type for each primal type.\n\nGiven a tangent t, you can get its fdata and rdata using f = fdata(t) and r = rdata(t) respectively. f and r can be re-combined to recover the original tangent using the binary version of tangent: tangent(f, r). It must always hold that\n\ntangent(fdata(t), rdata(t)) === t\n\nThe need for this split is explained in the docs; for now, just look at what fdata and rdata look like for our running examples.\n\nInt\n\nInts are non-differentiable types, so there is nothing to pass around on the forwards- or reverse-pass. Therefore\n\njulia> fdata_type(tangent_type(Int)), rdata_type(tangent_type(Int))\n(NoFData, NoRData)\n\nFloat64\n\nThe tangent type of Float64 is Float64. Float64s are identified by their value / have no fixed address, so\n\njulia> (fdata_type(Float64), rdata_type(Float64))\n(NoFData, Float64)\n\nVector{Float64}\n\nThe tangent type of Vector{Float64} is Vector{Float64}. A Vector{Float64} is identified by its address, so\n\njulia> (fdata_type(Vector{Float64}), rdata_type(Vector{Float64}))\n(Vector{Float64}, NoRData)\n\nVector{Float64} does not need rdata because it is the fdata that is incremented in-place during the reverse pass.\n\nTuple{Float64, Vector{Float64}, Int}\n\nThis is an example of a type which has both fdata and rdata. The tangent type for Tuple{Float64, Vector{Float64}, Int} is Tuple{Float64, Vector{Float64}, NoTangent}. Tuples have no fixed memory address, so we interrogate each field on its own. We have already established the fdata and rdata types for each element, so we recurse to obtain:\n\njulia> T = tangent_type(Tuple{Float64, Vector{Float64}, Int})\nTuple{Float64, Vector{Float64}, NoTangent}\n\njulia> (fdata_type(T), rdata_type(T))\n(Tuple{NoFData, Vector{Float64}, NoFData}, Tuple{Float64, NoRData, NoRData})\n\nThe zero tangent for (5.0, [5.0]) is t = (0.0, [0.0]). fdata(t) returns (NoFData(), [0.0]), where the second element is === to the second element of t. rdata(t) returns (0.0, NoRData()). In this example, t contains a mixture of data, some of which is identified by its value, and some of which is identified by its address, so there is some fdata and some rdata.\n\nStructs\n\nStructs are handled in more-or-less the same way as Tuples, albeit with the possibility of undefined fields needing to be explicitly handled. For example, a struct such as\n\njulia> struct Foo\n           x::Float64\n           y\n           z::Int\n       end\n\nhas tangent type\n\njulia> tangent_type(Foo)\nTangent{@NamedTuple{x::Float64, y, z::NoTangent}}\n\nIts fdata and rdata are given by special FData and RData types:\n\njulia> (fdata_type(tangent_type(Foo)), rdata_type(tangent_type(Foo)))\n(FData{@NamedTuple{x::NoFData, y, z::NoFData}}, RData{@NamedTuple{x::Float64, y, z::NoRData}})\n\nPractically speaking, FData and RData both have the same structure as Tangents and are just used in different contexts.\n\nMutable Structs\n\nThe fdata for a mutable structs is its tangent, and it has no rdata. This is because mutable structs have fixed memory addresses, and can therefore be incremented in-place. For example,\n\njulia> mutable struct Bar\n           x::Float64\n           y\n           z::Int\n       end\n\nhas tangent type\n\njulia> tangent_type(Bar)\nMutableTangent{@NamedTuple{x::Float64, y, z::NoTangent}}\n\nand fdata / rdata types\n\njulia> (fdata_type(tangent_type(Bar)), rdata_type(tangent_type(Bar)))\n(MutableTangent{@NamedTuple{x::Float64, y, z::NoTangent}}, NoRData)\n\nPrimitive Types\n\nAs with tangents, each primitive type must specify what its fdata and rdata is. See specific examples for details.\n\n\n\n\n\n","title":"DifferCore.fdata_type"},{"category":"function","location":"reference/#DifferCore.rdata_type","page":"API Reference","text":"rdata_type(T)\n\nReturns the type of the reverse data of a tangent of type T.\n\nExtended help\n\nSee extended help in fdata_type docstring.\n\n\n\n\n\n","title":"DifferCore.rdata_type"},{"category":"function","location":"reference/#DifferCore.zero_tangent","page":"API Reference","text":"zero_tangent(x)\n\nReturns the unique zero element of the tangent space of x. It is an error for the zero element of the tangent space of x to be represented by anything other than that which this function returns.\n\n\n\n\n\nzero_tangent(primal, fdata)\n\nEquivalent to tangent(fdata, zero_rdata(primal)).\n\nPrefer this two-argument form over the single-argument zero_tangent(primal) whenever primal is or contains a Ptr, since single-argument zero_tangent is not safe for pointer types (it will throw). The two-argument form handles pointer-containing types correctly by using zero_rdata, whose result for a Ptr field is just NoRData().\n\n\n\n\n\n","title":"DifferCore.zero_tangent"},{"category":"function","location":"reference/#DifferCore.zero_rdata","page":"API Reference","text":"zero_rdata(p)\n\nGiven value p, return the zero element associated to its reverse data type.\n\n\n\n\n\n","title":"DifferCore.zero_rdata"},{"category":"function","location":"reference/#DifferCore.randn_tangent","page":"API Reference","text":"randn_tangent(rng::AbstractRNG, x::P) where {P}\n\nRequired for testing. Generate a randomly-chosen tangent to x. Very similar to zero_tangent, except that the elements are randomly chosen, rather than being equal to zero.\n\n\n\n\n\n","title":"DifferCore.randn_tangent"},{"category":"function","location":"reference/#DifferCore.fdata","page":"API Reference","text":"fdata(t)::fdata_type(typeof(t))\n\nExtract the forwards data from tangent t.\n\n\n\n\n\n","title":"DifferCore.fdata"},{"category":"function","location":"reference/#DifferCore.rdata","page":"API Reference","text":"rdata(t)::rdata_type(typeof(t))\n\nExtract the reverse data from tangent t.\n\nExtended help\n\nSee extended help section of fdata_type.\n\n\n\n\n\n","title":"DifferCore.rdata"},{"category":"function","location":"reference/#DifferCore.tangent","page":"API Reference","text":"tangent(f, r)\n\nReconstruct the tangent t for which fdata(t) == f and rdata(t) == r.\n\n\n\n\n\n","title":"DifferCore.tangent"},{"category":"function","location":"reference/#DifferCore.build_tangent","page":"API Reference","text":"build_tangent(::Type{P}, fields...) -> tangent_type(P)\n\nBuilds a tangent for primal type P out of per-field tangents, in fieldnames(P) order. The moral equivalent of calling P's constructor, but for its tangent type — fields that tangent_type says are PossiblyUninitTangent get wrapped automatically, and trailing fields may be omitted to leave them uninitialized.\n\n\n\n\n\n","title":"DifferCore.build_tangent"},{"category":"function","location":"reference/#DifferCore.get_tangent_field","page":"API Reference","text":"get_tangent_field(t::Union{MutableTangent, Tangent}, i::Int)\n\nGets the ith field of data in t. The moral equivalent of getfield for MutableTangent, treating t.fields as if its entries were fields of t directly.\n\n\n\n\n\n","title":"DifferCore.get_tangent_field"},{"category":"function","location":"reference/#DifferCore.set_tangent_field!","page":"API Reference","text":"set_tangent_field!(t::MutableTangent{Tfields}, i::Int, x) where {Tfields}\n\nSets the ith field of the data in t to x. The moral equivalent of setfield! for MutableTangent, treating t.fields as if its entries were fields of t directly.\n\n\n\n\n\n","title":"DifferCore.set_tangent_field!"},{"category":"function","location":"reference/#DifferCore.increment!!","page":"API Reference","text":"increment!!(x::T, y::T) where {T}\n\nAdd x to y. If ismutabletype(T), then increment!!(x, y) === x must hold. That is, increment!! will mutate x. This must apply recursively if T is a composite type whose fields are mutable.\n\n\n\n\n\n","title":"DifferCore.increment!!"},{"category":"function","location":"reference/#DifferCore.set_to_zero!!","page":"API Reference","text":"set_to_zero!!(x)\n\nSet x to its zero element (x should be a tangent, so the zero must exist).\n\n\n\n\n\n","title":"DifferCore.set_to_zero!!"},{"category":"function","location":"reference/#DifferCore.as_tangent","page":"API Reference","text":"as_tangent(x)::tangent_type(typeof(x))\n\nReturns a value matching a primal type x projected to the tangent space.\n\nExamples:\n\njulia> as_tangent(1.0)\n1.0\n\njulia> as_tangent(1)\nNoTangent()\n\njulia> as_tangent(1.0 + im)\nTangent{@NamedTuple{re::Float64, im::Float64}}((re = 1.0, im = 1.0))\n\njulia> as_tangent(1 + im)\nNoTangent()\n\n\n\n\n\n","title":"DifferCore.as_tangent"},{"category":"function","location":"reference/#DifferCore.unit_tangent","page":"API Reference","text":"unit_tangent(x)::tangent_type(typeof(x))\n\nEquivalent to as_tangent(oneunit(x)), i.e. get the equivalent of oneunit(x) but converted to an appropriate tangent type (if such an object exists).\n\njulia> unit_tangent(10.0)\n1.0\n\njulia> unit_tangent(10.0 + im)\nTangent{@NamedTuple{re::Float64, im::Float64}}((re = 1.0, im = 0.0))\n\njulia> unit_tangent(1)\nNoTangent()\n\nNot every object (e.g. vectors) has a canonical oneunit, so these will error:\n\njulia> unit_tangent([1.0])\nERROR: MethodError: no method matching one(::Vector{Float64})\n\n\n\n\n\n","title":"DifferCore.unit_tangent"},{"category":"type","location":"reference/#DifferCore.Inactive","page":"API Reference","text":"Inactive\n\nMarks a value as held constant: no derivative is propagated to or from it. A carrier (Dual/CoDual) puts Inactive() in its shadow slot to say so, and the engines put it in an aggregate's shadow slot for a constant component.\n\nDistinct from NoTangent, which says the type has no tangent space. The two cannot be merged: fdata_type(NoTangent) === NoFData, which is also an active Float64's fdata, so NoTangent cannot encode constancy once a value is nested. Inactive is preserved by both fdata and rdata, so it survives into aggregates.\n\nInactive is a strong zero, and the two directions are not the same operation:\n\nincrement!!(Inactive(), y) === Inactive() — accumulating into a constant discards, by definition of having declared it constant.\nincrement!!(x, Inactive()) === x — a constant contributes nothing, leaving x untouched.\n\nSo increment!! is not commutative in the presence of Inactive: the first slot is the accumulator that owns storage, the second is a contribution. Both follow the general rule that increment!! returns a value of the accumulator's type.\n\nNoTangent deliberately keeps no absorbing arm, so a mis-analysed active value still raises a MethodError rather than silently dropping a gradient. Inactive absorbs only because it is written exactly where constancy was declared.\n\nBecause Inactive is a singleton, the no-op arms cost nothing: each activity signature compiles separately, with the dead work erased.\n\n\n\n\n\n","title":"DifferCore.Inactive"},{"category":"function","location":"reference/#DifferCore.fdata_shadow_type","page":"API Reference","text":"fdata_shadow_type(P::Type)\n\nThe types a reverse-mode shadow for a primal of type P may legally have: its ordinary fdata type, or Inactive if the value is held constant. This is what a CoDual's second slot admits.\n\nA validity predicate, never a declaration. Use it in <: checks and rule-signature constraints. Every declared slot, field, comms item and SSA type must stay concrete — the engines pick the concrete alternative from their own per-value activity, so no union ever reaches a hot path.\n\nSee tangent_shadow_type for the forward-mode half.\n\n\n\n\n\n","title":"DifferCore.fdata_shadow_type"},{"category":"function","location":"reference/#DifferCore.tangent_shadow_type","page":"API Reference","text":"tangent_shadow_type(P::Type)\n\nThe types a forward-mode shadow for a primal of type P may legally have: its ordinary tangent type, or Inactive if the value is held constant. This is what a Dual's second slot admits.\n\nForward mode carries the whole tangent rather than the fdata half, which is the only difference from fdata_shadow_type. The same \"validity predicate, never a declaration\" rule applies.\n\n\n\n\n\n","title":"DifferCore.tangent_shadow_type"},{"category":"function","location":"reference/#DifferCore.shadow_type","page":"API Reference","text":"shadow_type(P::Type)\n\nDeprecated alias for fdata_shadow_type. Named before forward mode carried Inactive, when there was only one shadow half to describe.\n\n\n\n\n\n","title":"DifferCore.shadow_type"},{"category":"function","location":"reference/#DifferCore.inactive_constant_type","page":"API Reference","text":"inactive_constant_type(P::Type) -> Bool\n\nWhether a statically-known code constant (IR literal, QuoteNode, const GlobalRef) of primal type P qualifies to be minted as Inactive() by the engines, rather than as an active zero tangent.\n\ntrue for primals that have a tangent space whose tangent is fdata-free — scalars, immutable float aggregates. Nothing can write to such a constant, so a zero shadow could only ever stay zero, and Inactive says the same thing for free.\n\nfalse in two cases:\n\nNoTangent primals (Int, function singletons, …), which already collapse to NoTangent at no cost and gain nothing from Inactive.\nanything with fdata — mutable state such as a global array. Its shadow is aliased storage that a write inside the differentiated function updates, so it keeps a zero shadow and gradients thread through a write-then-read.\n\nThis is the single place the policy lives. The engines compute the same predicate through their own world-age funnels rather than calling this at transform time, so those call sites must be kept in step with this definition.\n\n\n\n\n\n","title":"DifferCore.inactive_constant_type"},{"category":"function","location":"reference/#DifferCore.isactive","page":"API Reference","text":"isactive(dx)\n\nWhether the shadow dx carries a derivative — false exactly for Inactive().\n\nDecidable from the shadow's type in every position, which is the point of Inactive being its own type: an active Float64's shadow is NoFData(), indistinguishable from what a constant one would carry if constancy were encoded as an empty fdata.\n\n\n\n\n\n","title":"DifferCore.isactive"},{"category":"macro","location":"reference/#DifferCore.@ifactive","page":"API Reference","text":"@ifactive dx expr\n\nexpr if the shadow dx is active, NoRData() otherwise. Inactive is a singleton so the test folds away — the two activity specialisations of a rule compile separately, with no Union result.\n\nNoRData(), not Inactive(): activity has to be visible in the fdata half, where a slot declared at the primal-derived type would otherwise be a type lie. An inactive argument's rdata slot carries nothing either way, and NoRData() is what the derived path emits for one, so hand rules and derived rules return the same shape. tangent(Inactive(), NoRData()) reassembles to Inactive() regardless.\n\nFor a pullback component in a hand-written rrule!!:\n\nmul_pullback(dz) = (NoRData(), @ifactive(dx, dz * y), @ifactive(dy, dz * x))\n\n\n\n\n\n","title":"DifferCore.@ifactive"},{"category":"type","location":"reference/#DifferCore.Dual","page":"API Reference","text":"Dual(primal::P, tangent::T)\n\nUsed to pair together a primal value and a tangent to it. In the context of foward mode AD (aka computing Fréchet derivatives), primal governs the point at which the derivative is computed, and tangent the direction in which it is computed.\n\nT is one of two things:\n\ntangent_type(P), the ordinary case — forward mode carries the whole tangent, not the fdata half.\nInactive, marking the value as held constant by the caller. Legal for any P; tangent_shadow_type(P) is the pair of them.\n\nDual{P,Inactive} reaches frule!! only in argument position, but it does reach it: an inactive value consumed by an active call is handed to the rule as-is, so a hand rule can skip the term instead of multiplying by a zero. Consumers that read a tangent as a value — intrinsic and builtin rules, phi-likes, %new, the return — get a materialised zero at the value's definition instead. A rule never returns Inactive (see dualize_to_ircode's frule_split!).\n\n\n\n\n\n","title":"DifferCore.Dual"},{"category":"function","location":"reference/#DifferCore.primal","page":"API Reference","text":"primal(x)\n\nThe primal value carried by x — a Dual (forward mode) or a CoDual (reverse mode).\n\n\n\n\n\n","title":"DifferCore.primal"},{"category":"function","location":"reference/#DifferCore.frule!!","page":"API Reference","text":"frule!!(fdual::Dual, argduals::Dual...) -> Dual\n\nForward-mode rule for primal(fdual)(primal.(argduals)...), returning the result and its directional derivative together as a single Dual.\n\nHand-written primitives (see src/rules_math.jl for the shape to follow) are methods with a specific fdual/argduals shape; a composite function is handled by an @generated fallback that derives the rule from f's IR, so frule!! works on anything.\n\n\n\n\n\n","title":"DifferCore.frule!!"},{"category":"function","location":"reference/#DifferForwards.code_dual_ircode","page":"API Reference","text":"code_dual_ircode(f, argtypes::Tuple; order=1, world=Base.get_world_counter(), inactive=()) -> Pair{IRCode,Any}\n\nReturn ir => rettype, the optimized dualized IRCode for differentiating f at arguments of types argtypes (each primal type T is seeded with a Dual{T,T}; the function itself with Dual{typeof(f),NoTangent}). order requests a higher derivative by nesting all seeds uniformly to that depth — order=2 seeds each T with Dual{Dual{T,T},Dual{T,T}} and the function with Dual{Dual{typeof(f),NoTangent},Dual{typeof(f),NoTangent}} — which is useful for running Core.Compiler.verify_ir on the higher-order IR without a live call. inactive names argument positions (1-based, not counting f) the caller holds constant, seeding those with Dual{T,Inactive} — the same thing frule!!(…, Dual(x, Inactive())) does at a live call. Errors if dualization bails (e.g. array indexing — not yet supported).\n\nExamples\n\nir, rt = code_dual_ircode(x -> sin(x) + 1, (Float64,))\ncode_dual_ircode(*, (ComplexF64, ComplexF64))\ncode_dual_ircode(x -> x*x, (Float64,); order=2)   # second-derivative IR\n\nSee also @code_dual_ircode.\n\n\n\n\n\n","title":"DifferForwards.code_dual_ircode"},{"category":"macro","location":"reference/#DifferForwards.@code_dual_ircode","page":"API Reference","text":"@code_dual_ircode f(args...)\n\nConvenience macro: show the optimized dualized IRCode for the call f(args...), using the runtime types of args. Equivalent to code_dual_ircode(f, map(_typeof, (args...,))).\n\n@code_dual_ircode sin(0.5)\n@code_dual_ircode (1.0+2.0im) * (3.0+4.0im)\n\n\n\n\n\n","title":"DifferForwards.@code_dual_ircode"},{"category":"type","location":"reference/#DifferCore.CoDual","page":"API Reference","text":"CoDual{Tx,Tdx}\n\nA primal value paired with its shadow. Tdx is one of:\n\nfdata_type(tangent_type(Tx)) — the ordinary active carrier, built by fcodual_type;\ntangent_type(Tx) — the full-tangent flavour, built by codual_type;\nInactive — the value is held constant: no derivative is propagated to or from it.\n\nThe third case is what isactive tests, and it is decidable from Tdx alone. That is why it is Inactive rather than NoTangent: an active Float64's shadow is NoFData(), so an empty fdata cannot mean \"constant\", and NoTangent is already the tangent of a type with no tangent space.\n\nMarking an argument constant is a promise about aliasing: an inactive value must not share memory with an active one, and an active value must not be stored into an inactive container. Neither is checkable here.\n\n\n\n\n\n","title":"DifferCore.CoDual"},{"category":"function","location":"reference/#DifferCore.rrule!!","page":"API Reference","text":"rrule!!(fcd::CoDual, ctx::AbstractCtx, argcds::CoDual...) -> (ycd, pullback)\n\nReverse-mode rule for primal(fcd)(primal.(argcds)...), returning the result as a CoDual plus a pullback. Call the pullback with an rdata seed for the result to get the tuple of rdatas for (f, args...); fdata-carried gradients (arrays, mutable structs) are accumulated in place into the CoDuals' own shadows instead.\n\nHand-written primitives (see src/rrules.jl for the sin/cos rules and the shape to follow) are methods with a specific fcd/argcds shape; a composite function is handled by an @generated fallback that derives the rule from f's IR. ctx::AbstractCtx carries the tape (build a reusable one with build_ctx); a hand rule that needs no tape ignores it. A hand rule must declare its ctx slot as ::AbstractCtx (never a concrete subtype) — that's what keeps dispatch against the fallback unambiguous.\n\nContract for an fdata-carrying result (an array or mutable struct): the returned CoDual's shadow must be the exact object the rule's own pullback reads back, never a detached copy — a caller may accumulate into it in place before the pullback runs. Every hand-written array rule already follows this (rules_broadcast.jl, rules_reductions.jl, rules_indexing.jl, rules_linalg.jl).\n\n\n\n\n\n","title":"DifferCore.rrule!!"},{"category":"type","location":"reference/#DifferCore.AbstractCtx","page":"API Reference","text":"AbstractCtx\n\nSupertype of reverse-mode differentiation contexts — the argument rrule!! threads its per-call/per-task state through, chiefly the tape. Ctx is the default. Every rrule!! method dispatches this slot as ::AbstractCtx (never a concrete subtype), which keeps the derived-fallback-vs-hand-rule dispatch ambiguity-free.\n\n\n\n\n\n","title":"DifferCore.AbstractCtx"},{"category":"type","location":"reference/#DifferReverse.Ctx","page":"API Reference","text":"Ctx{P} <: AbstractCtx\n\nThe default differentiation context, carrying a pre-allocated tape in tape::P.\n\nP === Nothing means \"allocate a fresh tape on every call\" — the simple, stateless mode. A hand-written rule's callee always gets this (its pullback isn't a Tape, so there's nothing to pre-allocate for it). A derived (non-hand-ruled) nested or recursive inner call instead gets a recycled tape: its pullback is a value pushed onto the outer block's comms Stack once per execution, and a Stack never deallocates, so after the first execution the slot the next push lands in already holds a structurally identical tape — the emission site hands that one to the callee (via Ctx{P} with a concrete P, not Ctx{Nothing}) instead of allocating fresh (_inner_ctx, src/stack.jl). Any P other than Nothing is this shape: a tape whose stacks are reset and reused per call — either the caller's own top-level tape (build_ctx) or a callee's recycled one. Build a top-level one with build_ctx.\n\n\n\n\n\n","title":"DifferReverse.Ctx"},{"category":"function","location":"reference/#DifferReverse.build_ctx","page":"API Reference","text":"build_ctx(::Type{CDs}) -> Ctx\nbuild_ctx(fcd::CoDual, argcds::CoDual...) -> Ctx\nbuild_ctx(f, argtypes::Tuple; inactive=()) -> Ctx\n\nBuild a reusable differentiation context — a Ctx wrapping a tape sized for the derived rule of the call it describes (obtained by transforming the primal's optimized IR). Pass it to rrule!! / value_and_gradient! / rev_gradient!.\n\nCDs is the Tuple of CoDual types the call will be made with, the function's own carrier first — exactly rrule!!'s argument list minus the context. This is the primary form: everything the tape shape depends on, activity included, is already a type parameter, so the result type is a pure function of CDs with no const-folding involved.\n\nctx = build_ctx(Tuple{typeof(fcd),typeof(vcd),typeof(acd)})\n\nThe CoDual form is the same thing spelled with the carriers themselves, and is usually what you want when you have them to hand — it cannot disagree with the call, because it is the call's argument list:\n\nacd = CoDual(a, Inactive())            # held constant, stated once\nctx = build_ctx(fcd, vcd, acd)\ny, pb = rrule!!(fcd, ctx, vcd, acd)\n\nThe primal-types form is the convenience spelling for when no carriers exist yet; it derives each argument's carrier from its primal type.\n\n; inactive=(p, …) marks argument positions (1-based, arguments only) the caller holds constant: the tape is sized for the CoDual{P,Inactive} carriers those slots must arrive as. It is an Int or a tuple of them — nothing else, since the positions ride into _build_tape's generator as a Val type parameter and only those are constructible as one. It should also be a compile-time constant (a literal, or a locally-constructed constant the compiler can fold): one the compiler cannot fold still works at run time, but build_ctx's return type is then not inferable, and the adjacent rrule!! dispatches dynamically. The two carrier forms above have neither restriction.\n\nThe tape is allocated once, and its stacks are reset and reused on every call — the whole point of holding onto a context rather than differentiating afresh. That makes the context single-use at a time: it is not reentrant and not thread-safe, so give each task its own. For a context that allocates a fresh tape per call instead, construct Ctx() directly; there is no flag for it.\n\nA reused tape also holds onto the previous call's argument coduals (the pullback reaches primal argument values through them) — and so keeps their shadows alive — until the next call overwrites them. Drop the context to release them.\n\nctx = build_ctx(f, (Vector{Float64},))\ny, pb = rrule!!(zero_fcodual(f), ctx, CoDual(x, dx))\n_, gx = pb(1.0)\n\n\n\n\n\n","title":"DifferReverse.build_ctx"},{"category":"function","location":"reference/#DifferReverse.value_and_gradient!","page":"API Reference","text":"value_and_gradient!(ctx::AbstractCtx, fcd::CoDual, argcds::CoDual...) -> (y, (df, dx1, dx2, ...))\n\nPre-allocated reverse mode: the caller supplies the context (from build_ctx) and each argument's shadow. Returns the primal value y alongside the tangents.\n\nEach CoDual(x, dx) pairs an argument with the buffer its gradient is accumulated into; dx is zeroed on entry. For an argument whose tangent lives in fdata (an Array, a mutable struct) dx holds that argument's gradient when the call returns. For a scalar there is nothing to pre-allocate — pass zero_fcodual(x) and read the gradient out of the returned tuple.\n\nx, dx = [1.0, 2.0], zeros(2)\nctx = build_ctx(f, (Vector{Float64},))\ny, (_, gx) = value_and_gradient!(ctx, zero_fcodual(f), CoDual(x, dx))\ngx === dx   # true — accumulated in place\n\nSee also rev_gradient! and rev_gradient.\n\n\n\n\n\n","title":"DifferReverse.value_and_gradient!"},{"category":"function","location":"reference/#DifferReverse.rev_gradient","page":"API Reference","text":"rev_gradient(f, args...) -> (df, dx1, dx2, ...)\n\nReverse-mode gradient of f(args...) for scalar output. Allocates everything it needs (zero shadows for each argument, and a tape); see rev_gradient! for the pre-allocated form and build_ctx to hold onto a reusable context. public, not exported — DifferentiationInterface is the primary user-facing entry point, this is the direct one.\n\n\n\n\n\n","title":"DifferReverse.rev_gradient"},{"category":"function","location":"reference/#DifferReverse.rev_gradient!","page":"API Reference","text":"rev_gradient!(ctx::AbstractCtx, fcd::CoDual, argcds::CoDual...) -> (df, dx1, dx2, ...)\n\nPre-allocated form of rev_gradient. See value_and_gradient! for the full description; this drops the primal value.\n\n\n\n\n\n","title":"DifferReverse.rev_gradient!"},{"category":"function","location":"reference/#DifferCore.zero_fcodual","page":"API Reference","text":"zero_fcodual(x)\n\nEquivalent to CoDual(x, fdata(zero_tangent(x))).\n\nFor Ptr{P}, falls back to uninit_fcodual(x) for the same reason as zero_codual — the full tangent is fdata for Ptr, so the placeholder bitcast applies here too.\n\n\n\n\n\n","title":"DifferCore.zero_fcodual"},{"category":"function","location":"reference/#DifferCore.fcodual_type","page":"API Reference","text":"fcodual_type(P::Type)\n\nThe type of the CoDual which contains instances of P and its fdata.\n\n\n\n\n\n","title":"DifferCore.fcodual_type"},{"category":"function","location":"reference/#DifferCore.codual_type","page":"API Reference","text":"codual_type(P::Type)\n\nThe type of the CoDual which contains instances of P and associated tangents.\n\n\n\n\n\n","title":"DifferCore.codual_type"},{"category":"function","location":"reference/#DifferReverse.tape_type","page":"API Reference","text":"tape_type(f, argtypes::Tuple; world=Base.get_world_counter()) -> Type{<:Tape}\n\nThe concrete Tape type the reverse-mode forwards carrier for f at argtypes returns — i.e. the tape a real rrule!! call would build. Recovered from code_reverse_fwds_ircode's return type, exactly as code_reverse_pullback_ircode does it, so this reports the tape both carriers agree on.\n\nChiefly useful with comms_element_types for asserting on tape layout in tests.\n\n\n\n\n\n","title":"DifferReverse.tape_type"},{"category":"function","location":"reference/#DifferReverse.comms_element_types","page":"API Reference","text":"comms_element_types(TapeT::Type{<:Tape}) -> Vector{Any}\n\nThe element type of every per-block comms stack in TapeT that actually stores something, in block order. SingletonStack slots (blocks with nothing to communicate) are skipped — they carry no storage, so they say nothing about tape size.\n\nEach element is the Tuple type that block pushes once per execution. Assert on properties of the whole collection (all(isbitstype, …), sum(sizeof, …)) rather than on a particular block's index: block numbering shifts with any unrelated change to Julia's optimizer.\n\n\n\n\n\n","title":"DifferReverse.comms_element_types"},{"category":"function","location":"reference/#DifferReverse.code_reverse_fwds_ircode","page":"API Reference","text":"code_reverse_fwds_ircode(f, argtypes::Tuple; world=Base.get_world_counter()) -> Pair{IRCode,Any}\n\nReturn ir => rettype, the optimized IRCode for the reverse-mode forwards carrier (reverse_fwds_impl) differentiating f at arguments of types argtypes (each primal type T, and f itself, wrapped in fcodual_type). rettype is Tuple{CoDual, Tape} — the primal result plus the tape code_reverse_pullback_ircode needs. Reproduces exactly what CC.optimize installs, without going through the throwing carrier stub. Errors if the reverse-mode transform bails (see the scope notes in reverse_interp.jl's header).\n\nExamples\n\nir, rt = code_reverse_fwds_ircode(x -> x*x + x, (Float64,))\n\nSee also @code_reverse_fwds_ircode and code_reverse_pullback_ircode.\n\n\n\n\n\n","title":"DifferReverse.code_reverse_fwds_ircode"},{"category":"macro","location":"reference/#DifferReverse.@code_reverse_fwds_ircode","page":"API Reference","text":"@code_reverse_fwds_ircode f(args...)\n\nConvenience macro: show the optimized IRCode for the reverse-mode forwards carrier for the call f(args...), using the runtime types of args.\n\n\n\n\n\n","title":"DifferReverse.@code_reverse_fwds_ircode"},{"category":"function","location":"reference/#DifferReverse.code_reverse_pullback_ircode","page":"API Reference","text":"code_reverse_pullback_ircode(f, argtypes::Tuple; seedtype::Type=Float64, world=Base.get_world_counter()) -> Pair{IRCode,Any}\n\nReturn ir => rettype, the optimized IRCode for the reverse-mode pullback carrier (reverse_pullback_impl) for f at arguments of types argtypes. seedtype is the type of the rdata seed for the primal's return value (default Float64, for scalar output). The Tape type is recovered from code_reverse_fwds_ircode's own return type, so both carriers agree on it by construction (mirroring how they'd be produced by an actual rrule call).\n\nExamples\n\nir, rt = code_reverse_pullback_ircode(x -> x*x + x, (Float64,))\n\nSee also @code_reverse_pullback_ircode and code_reverse_fwds_ircode.\n\n\n\n\n\n","title":"DifferReverse.code_reverse_pullback_ircode"},{"category":"macro","location":"reference/#DifferReverse.@code_reverse_pullback_ircode","page":"API Reference","text":"@code_reverse_pullback_ircode f(args...)\n\nConvenience macro: show the optimized IRCode for the reverse-mode pullback carrier for the call f(args...), using the runtime types of args.\n\n\n\n\n\n","title":"DifferReverse.@code_reverse_pullback_ircode"},{"category":"type","location":"reference/#DifferForwards.AutoDifferForwards","page":"API Reference","text":"AutoDifferForwards <: ADTypes.AbstractADType\n\nSelects Differ's forward mode for DifferentiationInterface.jl. Method implementations live in DifferForwards.jl's own DifferForwardsDifferentiationInterfaceExt package extension, loaded when DifferentiationInterface is.\n\n\n\n\n\n","title":"DifferForwards.AutoDifferForwards"},{"category":"type","location":"reference/#DifferReverse.AutoDifferReverse","page":"API Reference","text":"AutoDifferReverse <: ADTypes.AbstractADType\n\nSelects Differ's reverse mode for DifferentiationInterface.jl. Method implementations live in DifferReverse.jl's own DifferReverseDifferentiationInterfaceExt package extension, loaded when DifferentiationInterface is.\n\n\n\n\n\n","title":"DifferReverse.AutoDifferReverse"},{"category":"type","location":"reference/#Contextual.ContextualInterpreter","page":"API Reference","text":"ContextualInterpreter{T,S} <: AbstractInterpreter\n\nA generic AbstractInterpreter that compiles a contextually transformed version of a primal method.\n\nThis is a source-to-source pass on the primal's post-optimization IRCode, installed into the normal typeinf pipeline so it produces an ordinary invoke-able CodeInstance. The concrete transform is supplied by a plugin (Differ's forward/reverse-mode engines) via the build_contextual_ir hook.\n\nowner::T is the plugin's immutable \"owner\" type — it IS the cache_owner partition key directly, so two egal-equal owners must always be safe to share a CodeInstance cache partition. custom_state::S is whatever mutable, per-session scratch state the plugin needs, deliberately kept out of T/cache_owner's reach.\n\n\n\n\n\n","title":"Contextual.ContextualInterpreter"},{"category":"function","location":"reference/#Contextual.build_contextual_ir","page":"API Reference","text":"build_contextual_ir(interp::ContextualInterpreter, mi::MethodInstance) -> Union{IRCode,Nothing}\n\nPlugin hook: build the transformed IRCode for a carrier MethodInstance, or return nothing to leave mi to the ordinary compilation pipeline. Overridden per plugin (DifferForwards recognizes its dualized_impl carrier, DifferReverse its reverse_fwds_impl/reverse_pullback_impl carriers). Called from finishinfer!, at the point Julia's inference machinery is about to freeze a method's return type into its CodeInstance; when it returns a real IRCode, that return type and the plugin's discovered backedges get written in its place, and run_ipo_passes! later optimizes it in optimize. A plugin whose transform can't handle some construct returns nothing here: the carrier's throwing stub body then compiles normally and raises when invoked, a graceful failure rather than a miscompile.\n\n\n\n\n\n","title":"Contextual.build_contextual_ir"},{"category":"function","location":"reference/#Contextual.run_ipo_passes!","page":"API Reference","text":"run_ipo_passes!(ir::IRCode, opt::OptimizationState) -> IRCode\n\nRuns the IPO-safe half of Julia's ordinary optimization pipeline (compact! → ssa_inlining_pass! → compact! → sroa_pass! → adce_pass! → compact!) over an already-SSA IRCode, skipping the CONVERT/SLOT2REG steps a freshly-lowered CodeInfo would need. Called from optimize on the IRCode build_contextual_ir produced, so the synthetic construction/rule calls it emits get inlined and immutable results scalar-replaced just like ordinary code.\n\n\n\n\n\n","title":"Contextual.run_ipo_passes!"},{"category":"function","location":"reference/#Contextual.expr_to_codeinfo","page":"API Reference","text":"expr_to_codeinfo(m::Module, argnames, spnames, sp, e::Expr, isva::Bool=false) -> CodeInfo\n\nBuilds a lowered CodeInfo from expression e, whose body returns e, with argument names argnames and static parameter names spnames. Used by a plugin's @generated fallback to produce the trivial generated body that invokes the compiled transformed CodeInstance.\n\n\n\n\n\n","title":"Contextual.expr_to_codeinfo"},{"category":"function","location":"reference/#Contextual.at_world","page":"API Reference","text":"at_world(world_or_interp, f, args...)\n\nCalls f(args...) with dispatch resolved at world (or at interp's inference world) instead of at the pin jl_call_staged puts on a @generated generator's task world age. Use for every call from transform code into a generic function another package can extend — ordinary dispatch from inside a generator can't see methods added by any later-loaded package (a sibling package, or a package extension). Every lookup made this way must also record an mt_edge! so a later method definition invalidates the carrier being built.\n\n\n\n\n\n","title":"Contextual.at_world"},{"category":"function","location":"reference/#Contextual.mt_edge!","page":"API Reference","text":"mt_edge!(edges::Vector{Any}, sig)\n\nRecords a method-table backedge on sig in edges, so a method appearing later for that signature invalidates the carrier being built. Pushes sig, Core.methodtable — the pair inline in the edge vector compute_edges!/store_backedges expects — deduplicated, since the same signature gets queried repeatedly per statement. Pair with every at_world lookup; a resolved MethodInstance edge is not a substitute, since it covers \"this method changed\" rather than \"a more specific method now exists\".\n\n\n\n\n\n","title":"Contextual.mt_edge!"},{"category":"section","location":"custom_rules/#Writing-custom-rules-for-Differ.jl","page":"Writing rules","text":"These docs are currently under construction, and need a lot more attention to be properly pedagogical. For now, I will attempt to show you the basics that you need to know to write custom rules for Differ.jl forwards and reverse mode.\n\nThe rule system is heavily based on Mooncake's rule system, with so if you are confused by something you see here, it may be good to go read https://chalk-lab.github.io/Mooncake.jl/stable/understandingmooncake/rulesystem/ . It might also be a good idea if you're new to AD in general to read a bit about ChainRules.jl https://juliadiff.org/ChainRulesCore.jl/stable/ since the documentation there is excellent, though note that ChainRules does have a slightly different model from Differ and Mooncake, and lacks mutation support.","title":"Writing custom rules for Differ.jl"},{"category":"section","location":"custom_rules/#Writing-an-frule!!-for-Forwards-mode-AD","page":"Writing rules","text":"Forwards mode AD is a bit simpler than reverse. Essentially, if we have some function \n\nfunction foo(x::Float64)\n    y = sin(x)\n    z = y^2\n    return z + 1\nend\n\nthen forwards mode automatic differentiation is concerned with calculating dfoo/dx by re-writing this into a new function of the form\n\nfunction DifferForwards.frule!!(::Dual{typeof(foo)}, (;x, dx)::Dual{Float64})\n    (; y, dy) = Dual(sin(x), var\"d(sin(x))/dx\" * dx)\n    (; z, dz) = Dual(y^2, var\"d(y^2)/dx\" * dy)\n    return Dual(z+1, var\"d(z+1)/dz\" * dz)\nend\n\nA Dual holds two objests: one represents the \"primal\" value in the regular function evaluation, and the other represents the derivative value. \n\nDiffer will automatically generate frule!! methods for a function like foo at compile time, but if there is a part of your calculation which Differ doesn't understand, but you know the derivative of, you can write a custom frule!! method to allow it to differentiate your function. \n\nFor example, if Differ didn't know the derivative of sin, you could write a custom method\n\nfunction DifferForwards.frule!!(::Dual{typeof(sin)}, (; x,dx)::Dual{Float64})\n    Dual(sin(x), cos(x)*dx)\nend\n\nand then it should automatically use your custom rule inside of the automatic derivative of foo. \n\nThis is harder to see with scalar code, but frule!! on Duals is essentially concerned with calculating a Jacobian-vector-product, i.e.\n\nfrule!!(::Dual(f, NoTangent()), Dual(a, b))\n\nwill calculate Jf(a) cdot b where Jf(a) is the Jacobian of f evaluated at a. By evaulating the frule!! across a whole basis, we can generate the whole Jacobian.\n\nHandling of mutation in forwards mode is also easy: if you mutate x, then you do the equivalent derivative mutations to dx. Suppose we have the function\n\nfunction f!(u, v) \n    u[1] = 2*v[1]^2\n    nothing\nend\n\nthen the appropriate frule!! (and indeed the one Differ would generate automatically) is\n\nfunction DifferForwards.frule!!(::Dual{f!}, udu::Dual, vdv::Dual)\n    u, du = udu.x, udu.dx\n    v, dv = vdv.x, vdv.dx\n    du[1] = 2 * (2v[1]) * dv[1]\n    nothing\nend","title":"Writing an frule!! for Forwards mode AD"},{"category":"section","location":"custom_rules/#Writing-a-rrule!!-for-reverse-mode-AD","page":"Writing rules","text":"Reverse mode AD is the transpose of forwards mode AD. Instead of calculating a JVP (Jacobian-vector-product) Jf(a) cdot b, reverse mode calculates the VJP (vector-jacobian product) b^dagger Jf(a). This is a trivial difference in simple programs, but it's enormously consequential for complicated programs.\n\nFor a function with many inputs and few outputs, the full jacobian can be reconstructed with fewer VJP evaluations than JVP evaluations, which is the reason that this technique is so impactful in fields like optimization where there is a scalar cost function, and unboundedly large parameter sets one may wish to differentiate with respect to.\n\nWhat does it mean to transpose a sequential program like foo? \n\nTo borrow some terminology from ChainRules, forwards mode AD is about taking a wiggle in the input space and asking how much that wiggle makes the output space wobble. Conversely, reverse mode AD is concerned with taking an already determined wobble in the output space, and asking how much wiggle that corresponds to in the input space. \n\nIn fancier language, if forwards mode calculates the pushforward on a function, then reverse mode is concerned with the pullback of that function.\n\nSo, what does reverse mode AD of foo look like?\n\nWe take our regular primal function, and have to run it backwards in order to propagate output wobbles into input wiggles.\n\nIn the simplest case with a function like sin, that just looks like\n\nfunction DifferReverse.rrule!!(::CoDual{typeof(sin),NoFData}, ctx::AbstractCtx, (; x)::CoDual{Float64,NoFData})\n    sinx, cosx = sincos(x)\n    fwd_result = CoDual(sinx, NoFData())\n    sin_pullback(dy) = (NoRData(), dy * cosx) # dy * d/dx sin(x)\n\n    fwd_result, sin_pullback\nend\n\nThis rrule!! takes in bunch of CoDual arguments (and a context argument ctx that we won't worry about for now), and then computes the primal result sin(x), and returns that together with a pullback function sin_pullback which computes the derivative of sin, multiplied by the wobble dy.\n\nWhat are these funky types NoFData and NoRData? Differ (like Mooncake which is where this convention comes from), splits information about derivatives into information that needs to be present during the forwards pass (evaluating the primal): FData, and information that's only needed during the reverse pass (evaluating the pullback): RData. Generally speaking, FData is where mutable structs and arrays go, and RData is where immutable objects go. This distinction is important when mutation comes into play later.\n\nNote that sin_pullback returns a Tuple with two members: NoRData() and the derivative w.r.t. x. A pullback must return a Tuple of outputs, one for each input to rrule!! including the function being differentiated. In this case, that's sin which has NoFData and NoRData so we know it's derivative is a NoTangent(). That's because sin is just a constant singleton and is not a differentiable argument. However, objects like closures can carry FData or RData in general, so this needs to be tracked. ","title":"Writing a rrule!! for reverse mode AD"},{"category":"section","location":"custom_rules/#rrule!!-for-a-function-with-multiple-steps","page":"Writing rules","text":"Okay, so that's a one-line program. What about something with multiple steps like foo? \n\nRepeating again our primal,\n\nfunction foo(x::Float64)\n    y = sin(x)\n    z = y^2\n    return z + x\nend\n\nAn rrule!! for this function would look like\n\nfunction DifferReverse.rrule!!(::CoDual{typeof(foo), NoFData}, ctx::AbstractCtx, (; x)::CoDual{Float64, NoFData})\n    # First we run the function fowards, evaluating the primal and collecting pullbacks:\n    (; y), y_pullback = rrule!!(CoDual(sin, NoFData()), ctx, CoDual(x, NoFData()))\n    (; z), z_pullback = rrule!!(CoDual(^,   NoFData()), ctx, CoDual(y, NoFData()), CoDual(2, NoFData()))\n    (; w), w_pullback = rrule!!(CoDual(+,   NoFData()), ctx, CoDual(z, NoFData()), CoDual(1, NoFData()))\n    \n    # and we combine this with a function that generates the pullback, running through the program backwards\n    function foo_pullback(dw)\n        (_, dz, _) = w_pullback(dw)\n        (_, dy, _) = z_pullback(dz)\n        (_, dx)    = y_pullback(dy)\n        return (NoRData(), dx)\n    end\n    return CoDual(w, NoFData()), foo_pullback # rrule!! returns the result of foo(x) (as a CoDual), together with the pullback closure\nend\n\nHere it becomes a bit more clear why it's called reverse mode AD. The pullback essentially runs the function backwards. \n\nOf course, if someone were to actually write a rrule!! for foo by hand, this could all be simplified significantly to a one-liner, but I split everything to show what Differ itself does when it encounters this function.","title":"rrule!! for a function with multiple steps"},{"category":"section","location":"custom_rules/#rrule!!-for-a-mutating-function","page":"Writing rules","text":"What about mutation? Well, bringing back our friend f!, it's time to meet FData for the first time:\n\nfunction f!(u, v) \n    u[1] = 2*v[1]^2\n    nothing\nend\n\nThis function has a rrule!! that looks like\n\nfunction rrule!!(::CoDual{typeof(f!),NoFData}, ::AbstractCtx,\n                 ucd::CoDual{Vector{Float64},Vector{Float64}},\n                 vcd::CoDual{Vector{Float64},Vector{Float64}})\n    u, du = primal(ucd), tangent(ucd)\n    v, dv = primal(vcd), tangent(vcd)\n\n    old_u1  = u[1]   # save the initial value of u\n    old_du1 = du[1]  # save the initial value of du\n    \n    u[1] = (2 * v[1])^2 # run the primal function\n    \n    du[1] = 0.0   # zero out the derivative of du, we're going to accumulate into this buffer\n\n    function f!_pullback(::NoRData)          # f! returns `nothing`, so there is *no* wobble in the output\n        dv[1] += 4 * v[1] * du[1]            # Derivative of the primal, mutating dv\n\n        u[1]  = old_u1  # undo mutation of u\n        du[1] = old_du1 # undo the mutation of du \n\n        return NoRData(), NoRData(), NoRData()   # f! is a constant, and u and v are tracked  purely through `FData`\n    end\n    return CoDual(nothing, NoFData()), f_pullback\nend","title":"rrule!! for a mutating function"},{"category":"section","location":"custom_rules/#Packaging-rules-for-another-package","page":"Writing rules","text":"Rules for a package Differ doesn't depend on belong in a package extension, so that loading Differ doesn't drag that package in. Both carriers (Dual, CoDual) and both rule functions (frule!!, rrule!!) are owned by DifferCore, so a single DifferCore-keyed extension covers both modes. That's how the SpecialFunctions.jl rules are packaged:\n\n# DifferCore/Project.toml\n[weakdeps]\nSpecialFunctions = \"276daf66-3868-5448-9aa4-cd146d93841b\"\n\n[extensions]\nDifferCoreSpecialFunctionsExt = \"SpecialFunctions\"\n\n# DifferCore/ext/DifferCoreSpecialFunctionsExt.jl\nmodule DifferCoreSpecialFunctionsExt\n\nusing SpecialFunctions\nimport DifferCore: Dual, frule!!, isactive, zero_tangent\n\nfunction frule!!(::Dual{typeof(erf)}, (; x, dx)::Dual)\n    y = erf(x)\n    isactive(dx) || return Dual(y, zero_tangent(y))\n    Dual(y, 2*exp(-x^2)/sqrt(π)*dx)\nend\n\nend\n\nNothing has to be registered anywhere: whether a hand rule exists is resolved through the ordinary method table every time a call is transformed, so rules that only appear part-way through a session still invalidate the derivatives compiled before them.\n\nDifferCore/ext/DifferCoreSpecialFunctionsExt.jl is worth reading in full. It covers most of SpecialFunctions' real scalar functions, and shows the two cases a rule for a real library has to get right. The first is an argument the caller holds constant (Inactive, above). The second is an argument no derivative is implemented for — a Bessel order, an incomplete gamma parameter — which neither mode reports as a silent zero. Both refuse it the same way and at the same point: constancy is visible in the shadow slot by the time a rule runs, so a slot carrying a live shadow is a caller genuinely asking for the missing derivative, and the rule raises an error at the forwards call. A constant in that slot — a literal, or an argument declared constant — arrives as Inactive, contributes nothing, and never reaches the refusal.","title":"Packaging rules for another package"},{"category":"section","location":"#Differ.jl","page":"Home","text":"Differ is a (highly experimental) attempt at a Julia-level automatic differentiation engine over optimized, typed IR. It is partially descended from Mooncake, in particular it inherits Mooncake's FData/RData/Tangent system with minor tweaks, and while the actual compiler passes are not based on Mooncake's exact designs, a lot of inspiration and lessons were taken from how Mooncake's compiler passes were written. In future work, I hope to encorporate more of Enzyme's compiler-level analysis and optimization passes in order to better approach its performance.\n\nThat largest Difference between Mooncake and Differ, is that Differ's code-generation phase happens at compile-time inside a @generated function, and hooks into the relatively new mechanism for invokeing CodeInstances, as such it currently only supports julia version 1.13 currently. This builds on and extends my previous stalled attempts to move Mooncake's rule-generation phase to compile time, https://github.com/chalk-lab/Mooncake.jl/pull/593 and https://github.com/chalk-lab/Mooncake.jl/pull/900 .\n\nDiffer is generally slower than Enzyme, and faster than Mooncake, though there are exceptions. It is far less mature and less tested than either.\n\nThis is a project mostly made for fun, and is not something serious to be relied upon.","title":"Differ.jl"},{"category":"section","location":"#Assorted-things-to-know","page":"Home","text":"A minority of this code is hand written by me, I mostly worked on the main compiler mechanism and entrypoints, but large sections of the overall codebase are on various points along the spectrum from \"vibe-coded\" to \"LLM-assisted\". Skepticism is very warranted.\nDiffer has forwards and reverse mode.\nBoth modes support mutation (though lack supprot for things like push!/pop!)\nForwards mode is simpler and more robust\nReverse mode works on a lot of common programs, but is more liable to choke. It will try to tell you what part of your program it doesn't understand.\nNested forwards-mode differentiation usually works. Forwards-over-Reverse differentiation works in limited case.","title":"Assorted things to know"},{"category":"section","location":"#Installing-Differ","page":"Home","text":"Differ currently has a bunch of sublibraries, none of which are registered. The easiest way to try it out is\n\nusing Pkg; Pkg.add([\n    PackageSpec(url=\"https://github.com/MasonProtter/Differ.jl\", subdir=\"Contextual\")\n    PackageSpec(url=\"https://github.com/MasonProtter/Differ.jl\", subdir=\"DifferCore\")\n    PackageSpec(url=\"https://github.com/MasonProtter/Differ.jl\", subdir=\"DifferForwards\")\n    PackageSpec(url=\"https://github.com/MasonProtter/Differ.jl\", subdir=\"DifferReverse\")\n    PackageSpec(url=\"https://github.com/MasonProtter/Differ.jl\")\n])\n\nRemember, Differ only works on julia v1.13","title":"Installing Differ"},{"category":"section","location":"#Using-via-DifferentiationInterface.jl","page":"Home","text":"The most ergonomic way to Differ is with DifferentiationInterface.jl, I have not yet upstreamed dispatch structs to ADTypes.jl, but Differ exports it's own structs AutoDifferForwards() and AutoDifferReverse().\n\njulia> using DifferentiationInterface, Differ\n\njulia> derivative(AutoDifferForwards(), 1.0) do x\n           sin(x)^2 + 1\n       end\n0.9092974268256818\n\nBecause Differ.jl does compile-time rule generation, these derivatives can be fast even without prepare_gradient machinery:\n\njulia> @btime derivative(AutoDifferForwards(), x) do x\n           sin(x)^2 + 1\n       end setup=(x=rand())\n  19.868 ns (0 allocations: 0 bytes)\n0.9927783746649702\n\nthough it's still mildly helpful for reverse mode where we allocate a Tape value:\n\njulia> f(v) = sum(sin, v);\n\njulia> v = rand(100);\n\njulia> @btime gradient(f, AutoDifferReverse(), $v);\n  3.244 μs (54 allocations: 9.59 KiB)\n\nversus\n\njulia> prep = prepare_gradient(f, AutoDifferReverse(), v);\n\njulia> @btime gradient(f, $prep, AutoDifferReverse(), $v);\n  2.356 μs (2 allocations: 928 bytes)","title":"Using via DifferentiationInterface.jl"},{"category":"section","location":"#More-native-'API'","page":"Home","text":"Differ.jl's internal entrypoints are invoked by calling frule!! with Dual arguments, or rrule!! with CoDual arguments.\n\nThis computes the derivative 1.0 * d/dx sin(x) at x=5.0 using forwards mode AD:\n\njulia> f(x) = sin(x)^2 + 1;\n\njulia> fdf = Dual(f, zero_tangent(f))\nDual{typeof(f), NoTangent}(f, NoTangent())\n\njulia> xdx = Dual(5.0, 1.0)\nDual{Float64, Float64}(5.0, 1.0)\n\njulia> ydy = frule!!(fdf, xdx)\nDual{Float64, Float64}(1.9195357645382263, -0.5440211108893698)\n\nThis computes the gradient 1.0 *  ∇g(v) at v = [1.0, 2.0, 3.0] using reverse mode AD:\n\njulia> g(v) = sum(sin, v);\n\njulia> gdg = CoDual(g, NoFData()); # g and any mutable data needed to calculate its derivative\n\njulia> v = [1.0, 2.0, 3.0] # where we differentiate\n       dv = [0.0, 0.0, 0.0]# accumulator for gradient\n       vdv = CoDual(v, dv) # v together with gradient accumulator\nCoDual{Vector{Float64}, Vector{Float64}}([1.0, 2.0, 3.0], [0.0, 0.0, 0.0])\n\njulia> ctx = build_ctx(g, (Vector{Float64},)); # this pre-allocates a tape. If you don't want to do that, just do `Ctx()` instead.\n\njulia> res, pullback = rrule!!(gdg, ctx, vdv);\n\njulia> res # this is just g(v)\nCoDual{Float64, NoFData}(1.8918884196934453, NoFData())\n\njulia> pullback(1.0) # compute the pullback, modifying vdv\n(NoRData(), NoRData())\n\njulia> dv # dv was mutated in place by `pullback`\n3-element Vector{Float64}:\n  0.5403023058681398\n -0.4161468365471424\n -0.9899924966004454","title":"More native 'API'"}]
}
